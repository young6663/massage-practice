# 交接說明（給家裡的筆電）

最後更新：2026-09-23 下班前（公司電腦）

## 一句話現況

網站功能都做好了，在本機示範模式可以完整操作。**還沒有推到 GitHub，也還沒有建立 Google 試算表**，所以還沒上線，同學也還看不到。

## ⚠ 晚上開工前，先看最下方「完成紀錄」

- **有寫「公司電腦已完成，可以開始」** → 直接開工。
- **還是空的** → 公司電腦的 Claude 還在修改檔案。請先不要讓家裡的 Claude 改任何檔案，只能讀；過一陣子再看。兩邊同時改，OneDrive 會產生衝突的複本檔。

## 回家打開前先確認

1. 等 OneDrive 同步完成（工作列 OneDrive 圖示不再轉圈）再開這個資料夾。資料夾裡有 `.git`，同步到一半就操作容易出錯。
2. 不要兩台電腦同時開著這個專案工作。
3. 在 Claude Code 開新 session 時，**工作資料夾選「乙級上岸衝次營」**（不要選家目錄），這樣專案的子代理設定和本機伺服器設定才會生效。
4. 開頭跟 Claude 說：「請讀交接說明.md 和 docs/PROGRESS.md，接著做」。

## 公司電腦下班時的狀態

- 全部已存檔（Git），沒有未存檔的修改。存檔點：foundation、selection-mvp、practice-history、weak-questions、integrations、apps-script-backend、accessibility-review。
- 無障礙審查的 7 項嚴重問題已全部修正並存檔（存檔點 `accessibility-review`），通行碼錯誤訊息也已改好。

## 回家要做的事（依順序）

### 1. 推到 GitHub 並開網站（需要您登入 GitHub）

尚待您決定：repo 名稱與是否公開。免費帳號**只有公開 repo 能開 GitHub Pages 網站**。程式碼裡沒有同學名字（真實名單只在 `private/` 資料夾與試算表，`private/` 不會被推上去）。

建議名稱：`massage-practice` → 網址會是 `https://young6663.github.io/massage-practice/`

步驟（Claude 可以幫忙下指令，建立 repo 的網頁操作請您自己按）：
1. 在 GitHub 網站建立新的空白 repository（不要勾選加 README）。
2. 讓 Claude 執行：`git remote add origin https://github.com/young6663/<名稱>.git`，再執行 `git push -u origin main --tags`。
3. repo 的 Settings → Pages → Source 選「Deploy from a branch」，分支選 `main`、資料夾選 `/ (root)` → Save。
4. 約 1–2 分鐘後打開網址確認。

此時網站還是**示範模式**：每個人的資料只存在自己的瀏覽器，同學之間看不到彼此。要做完第 2 步才是真正共用。

### 2. 建立 Google 試算表與 Apps Script（需要您本人）

照 `apps-script/DEPLOY.md` 一步一步做（免費，約 10–15 分鐘）。重點：
- Google 授權畫面（含「Google 尚未驗證這個應用程式」→ 進階 → 前往）要您自己按。
- 群組通行碼（至少 6 個字）由您自己輸入，**不要告訴 Claude**，只口頭告訴同學。
- 匯入初始成員時，貼上 `private/seed.members.json` 的內容（成員甲、成員乙與第一天的 4 筆選題）。
- 部署完成拿到的網址（結尾是 `/exec`）交給 Claude，填進 `assets/js/config.js`，並把 `backend` 改成 `'appsScript'`，存檔後再推一次 GitHub。

### 3. 真人報讀實測（需要您本人）

照 `docs/A11Y_REVIEW_2026-09-23.md` 第 3 節：NVDA 15 步、iPhone VoiceOver 15 步。有問題直接告訴 Claude。

## 尚待決定

- 要不要讓術科網站支援「直接開到某一題」（需要修改術科網站本身，約 5 行，要您同意才做；不做也能用，目前開首頁）。

## 文件地圖

| 文件 | 內容 |
|---|---|
| `CLAUDE.md` | 給 Claude 的專案規則（精簡） |
| `docs/PROGRESS.md` | 目前進度、下一步、已知問題、重要決策 |
| `docs/PROJECT_SPEC.md` | 完整規格：資料模型、頁面、規則、安全 |
| `docs/ACCESSIBILITY.md` | 全盲＋低視能設計規格與測試清單 |
| `docs/INTEGRATIONS.md` | 與術科網站、經穴教練的連結方式 |
| `docs/MODEL_STRATEGY.md` | Opus／Sonnet／Haiku 分工 |
| `docs/A11Y_REVIEW_2026-09-23.md` | 無障礙審查報告與手動測試步驟 |
| `apps-script/DEPLOY.md` | 試算表與後端部署步驟 |

## 本機試用（不用網路後端）

家裡筆電如果有 Python：Claude 會用本機伺服器開 `http://127.0.0.1:8765/`。示範通行碼是 `1234`（只在示範模式有效）。

## 完成紀錄

**公司電腦已完成，可以開始。**（2026-09-23 晚上）

- 無障礙修正：嚴重 7 項全修；建議 13 項修 10 項，未修 3 項的原因在 `docs/PROGRESS.md`。
- 通行碼失效訊息改為「請按頁首的『切換身份』，重新輸入群組通行碼」。
- 修好測試頁在「全新瀏覽器、沒有任何資料」時找不到題目資料的問題。
- 驗證結果：單元測試 49 項全過、API 契約測試 12 項全過、axe 自動檢查 9 頁 0 違規、手機寬度無水平捲動。
- 公司電腦上的 Claude 已停止修改，沒有進行中的工作。
- 下一步：照上方「回家要做的事」第 1 步開始（GitHub）。

**2026-09-24 第 1 步（GitHub）已完成。**
- 公開 repo：https://github.com/young6663/massage-practice ，網站：https://young6663.github.io/massage-practice/ （示範模式，已實測首頁與選擇身份頁可開、無錯誤）。
- 公開前把文件中的兩位成員暱稱換成「成員甲／成員乙」，並把 Git 紀錄壓成單一新起點；舊紀錄與 7 個存檔點標籤備份在 `private/backups/history-before-squash-20260924.bundle`（不進 Git）。
- 下一步：第 2 步 Google 試算表與 Apps Script（需要本人）。

**2026-09-24 第 2 步（試算表＋Apps Script）已完成，網站已切換正式後端。**
- 試算表「乙級上岸練習系統」建在 junyoung.feng@gmail.com；10 個工作表、40 題、5 天已建好；通行碼由使用者本人設定（Claude 不知道）。
- 使用者決定**不匯入** `private/seed.members.json`（舊名單作廢），改由同學在 who.html 輸入通行碼後自己加入、自己選題。
- 部署 v1（執行身分：我；存取：所有人），網址已填入 `assets/js/config.js`。
- 網站新增版本號 `APP_VERSION` / `APP_VERSION_DATE`（config.js），每頁最下方顯示；之後每次修改都要遞增並更新日期。
- 尚未做：`tests/contract.html?backend=appsScript` 契約測試（需通行碼，會寫入測試資料）；NVDA／VoiceOver 真人實測。
